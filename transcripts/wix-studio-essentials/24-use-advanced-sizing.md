# 24. Use Advanced sizing with Wix Studio

- Video: https://www.youtube.com/watch?v=iQy7nC6b90Q
- Duration: 9:49
- Channel: Wix Studio

## Transcript

foreign [Music] most elements in the studio editor have some kind of default responsive Behavior you can check out which behaviors are available for each element and set them here in this drop down menu and while the size of your elements are represented as pixel values on canvas behind the scenes the studio editor uses a range of measurements like percentage viewport width and height and more to keep your content behaving the way you want but sometimes you might want to take control of those measurements yourself the measurement units you choose affect the responsive behavior of your elements on each screen size so understanding which measurement units achieve which behaviors can be crucial to creating the design you want so in this lesson we'll talk through the advanced sizing option starting with how to turn it on and how to use each of its Advanced measurements [Music] you can switch individual components and elements to Advanced sizing by clicking the more actions icon here and switching the toggle on or with the element selected you can just press option and I just a heads up you can only switch components back and forth one at a time you can't turn on Advanced sizing for your entire site at once when you turn on Advanced sizing on a component it'll make the measurement units visible for that component on all breakpoints all of the default responsive behaviors are built from the units of measurements you can see here their CSS properties so you can change them across breakpoints just like layouts position and colors now you can go to any breakpoint and change these to a set of measurements that aren't even covered by a default Behavior option then if you then go back to desktop and turn Advanced sizing back off it'll be turned off for that element on every breakpoint but turning it off won't change your work instead on mobile where we customize the measurements you'll see mixed think of it as an inspector panel setting that just shows the editor in a different way so with this switched off when you change your responsive behavior on a lower breakpoint you'll create an override disconnecting that element's responsive behavior from its properties on higher breakpoints the same is true when you change its behavior and advanced sizing mode so when you change the advanced properties to custom properties you'll see the mixed responsive Behavior selected you can still switch from mixed to a default Behavior but remember that will override any of your previous changes in advanced sizing with the units of that behavior for now we want to reconnect all the behaviors across breakpoints so we'll right-click the element and click use on all breakpoints [Music] in advanced sizing there are a handful of measurement units you can use to tell an element how it should behave and they each serve a different purpose here for example you'll see this element is set to fixed so that no matter what size the screen is it'll stay the same size now if we switch to Advanced sizing you'll see it still behaves the same way because it was actually set to pixels in both width and height you might use pixels when you want an element to stay the same size across devices even when other elements scale up or down these are standard pixels as opposed to the pixel on canvas measurements we've discussed before while elements set with pixel stay exactly the same other measurements are based on other things like the width of the parent container percentage the viewport height or viewport width the size of the children and more so we can use relative units of measurement to make sure that our designs look perfect on any screen size that's how the scale proportionally Behavior works so if we apply that here and resize our element it'll scale in proportion to its parent container but now let's say you want this image to be 70 of the Grid's containers width you could work it out for yourself in pixels on the canvas or just switch to Advanced sizing and you'll see here that it's actually just setting this container to percentage width anyway percentage units determine an element's size relative to its parents width or height or the grid areas placed in depending on how you've set it up so rather than calculating 70 in pixels and then entering that in pixels on canvas we can just set the width to 70 percent over here in the inspector you'll see this check box for scale proportionally if this is checked your width will always be in percentage and the element takes an aspect ratio so it scales proportionally if we switch from Auto to a different unit or if we toggle this off we'll remove the measurement units that created the proportional scaling Behavior if we were to switch both width and height to percentage the image would retain its size relative to its parents container the viewport is the size of the area where the site content is shown when you use viewport width or VW or viewport height VH your element will scale relative to the viewport's width or height viewport units are measured out of 100 so 50 VW means the element will take up half of the viewport width and elements always take their values from the viewport independent of their parent take this element for example let's set it to 70 percent you can see that the button's width now takes up 70 percent of its parent containers width but watch what happens when you change this to 7dbw now it takes up seventy percent of the entire viewports with ignoring the parent container size for now we don't want this overflow so let's put this button back to thirty percent you can set a VW and VH for every element so if you wanted to you could have an element take both its width and height from the viewport's width now the element will scale proportionally across breakpoints like this but for now let's go back to the previous measurements we had VH works the same way but takes the measurements from the viewport's height let's say we want to make this section take up the user's entire screen we can set its height to 100 VH but if our header is pinned it'll overlap with that section to have the section cover the entire viewport height but start exactly under where the header ends you can do that by setting the section height to another measurement unit calc calc allows us to set a measurement based on a calculation to get the effect we want here we need the section height to be equal to the entire viewport minus the header's height clicking on the header we can see that it's set to 100 pixels so the section needs to be set to 100 VH minus 100 pixels so now you can see that the header and the section together take up the entire height of the screen and this will still be true even at different screen sizes but let's say this logo is getting a bit too small now we can make sure it stays a good size using minimum and maximum measurements here so if we set this as the minimum width the logo will stop scaling once it reaches this size on any screen but if we scale the screen size up the logo still scales with it if you don't want it to be too large set the max limit so it stops growing at a certain point now let's move on to auto if an element is set to Auto its height is determined by its children by default Auto also means that as the container's content gets bigger or smaller the container changes with it and its height will change according to any margins that we add we can set a minimum height for the container if the container has no content inside it to take its height from it would collapse to zero but the minimum height prevents that to make sure the Box never gets too large or too small we can just add minimum and maximum measurements this way even if we remove this content from the container it will never snap to zero height and last up Max content just tells a parent element to match the widest or highest point of the content inside right now the text wraps it doesn't have any line breaks so basically when we change the container's width to Max content the text will be just one long line and the container will be as wide as the text so we've covered how to use Advanced sizing next up we'll go over how you can turn a section grid into a CSS grid and some ways you might use CSS grids to take your site to the next level see you there
