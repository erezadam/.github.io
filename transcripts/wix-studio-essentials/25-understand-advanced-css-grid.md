# 25. Understand advanced CSS Grid

- Video: https://www.youtube.com/watch?v=c_IwJMAlWdY
- Duration: 12:30
- Channel: Wix Studio

## Transcript

foreign [Music] let's talk about the difference between a section grid and CSS Grid in the studio editor with the section grid your measurements are all calculated for you but with the CSS grid you can control your Advanced measurement units to take more control over the responsive behaviors and the cells of the CSS grids aren't containers like they are on Section grids instead they're made up of columns and rows that direct your elements on how to behave across screen sizes unlike with Section grids where you can't attach an element to two grid cells at the same time and a CSS grid you can this might be useful when you have a lot of elements overlapping that don't necessarily all fit in the same grid structure plus with the CSS grid you can add an unlimited number of grid lines while section grids have a maximum of 16 grid cells and while you can change the layout of section grids on Lower breakpoints CSS grids give you much more control to have a completely unique layout across breakpoints and maybe the biggest difference section grids can only include responsive Behavior defaults so if you want different rows and columns to behave differently across screen sizes like one column row being fixed while the rest scale proportionally you'll need to use a CSS grid instead CSS grids can be a game changer for laying out containers items in a section grid items and repeaters multi-state boxes and more so in this lesson we'll cover how CSS grids can help enhance your page designs but first let's get started with how you can add and apply a CSS grid to your pages [Music] there are plenty of ways to add or switch to a CSS grid let's start by adding a section grid with two columns you can add a CSS grid to any cells in a section grid and remember section grids are just made up of containers so the same goes for regular containers too in this case let's add a CSS grid to this section grid cell just click on the item and go to the layout panel on the inspector and next to CSS grid click apply choose your starting layout from the floating panel you can even set a custom layout here but let's set this grid to 2x2 for now when you do this you'll be switching from responsive defaults to Advanced sizing so you'll see the actual measurements for these rows and columns depending on the responsive default you're using these measurements will look a little different in this case its columns are measured in Fr or fractional units which we'll get into in just a few minutes right now our row minimum is set to Max content and our Max is set to 50 percent Max content sets an element's height based on the size of its child elements in this case Max content tells the rows height to fit the content's current size you can edit all these measurements in the inspector so let's attach an image to this cell now let's hit stretch which will make the image fill whichever cells is attached to we'll attach it to the top left on the canvas but we can actually change its Associated cell position in the inspector here or we can hold shift and span it across two grid areas that's what we'll do here then we can change the container background to Black next up let's add a title to the top row but we'll have it go across both columns in the inspector you'll see it's now attached to the two top grid cells finally We'll add a paragraph to the bottom left and a button to the bottom right now the cells are acting as the parents of these elements so instead of dragging them around to reposition them we can use the alignment tools we want this top row to always be the same height as the text to do that let's click the rows measurements in the inspector and then change this minimum maximum to Max content now you can see that the top row got smaller and the bottom row shifted up with it leaving a gap at the bottom this is because our second Row's Max size is set to 50 percent meaning it'll only ever grow to 50 percent of the entire section's height but we can fix this with that FR measurement unit so let's change to FR and see what happens all of your fractional units on a grid added together represent the total available fractional space in the grid you'll notice that both of our columns are set to fractional that means on any screen they'll take up half of the fractional width available so 1fr is equal to one half of the space that we've set to FR according to its parent and down here the row is also set to 1 fr but its height value is clearly larger than the width of the two columns this bottom row is the only row set to FR so it'll take up all the space remaining after accounting for the other rows measurement we'll look at another example of that in a second now down in the mobile breakpoint the desktop layout doesn't quite work we can start by changing the section grid from columns to rows with this drop down in the floating panel let's change the item grid layout into one column with three rows by clicking the CSS grid going to the drop down and adding custom values here we can see that the title is placed in the first row now let's place the paragraph and button in the second row by selecting them and choosing the second row in the inspector then we'll place the image in the third row now our rows have different measurement units so we'll want to change those to make sure the grid fits the content that's in it let's set the first row to Max content so it'll always match the title height on any screen size and we'll set the second and third rows to FR so both rows will split the remaining grid height between them here's what's happening an algorithm adds up all the available height in the entire grid to get a measurement for all available space then it removes any height measurements including rows gaps margins and padding that aren't measured in Fr from its total measurement whatever's left over it divides equally by the number of rows that are measured in Fr ight now we've got two rows measured in Fr and they're both the same height so they're both equal to 1 fr it's doing the same calculation on the width at any screen size this row is still being calculated as one fraction of the available fractional space these measurement units make it so that whatever your Grid's height is it'll always be filled so if we want to make the grid taller we can just change the section height in the inspector and the grid will follow so let's add some gaps to the cells and margins to our content to provide more breathing space alright now let's see how all of this looks on different mobile screens with the resize handles [Music] back on desktop let's add a fixed column to the left of this section with the section grid we can't use different responsive behaviors on different cells columns or rows to do that we'll need to convert the entire section grid into a CSS grid by right-clicking the section and hitting switch to CSS grid you'll see this warning telling you that once you switch to CSS grid you won't be able to switch back to a section grid so let's click switch to CSS grid just like when we switch to single cell now that we've switched the entire grid to a CSS grid we'll see its Advanced measurements we can see that the cell from the section grid converts to a container within the CSS grid you can delete them and it won't affect the grid we created and like before the measurement units you see will be different depending on the responsive behaviors that you've set for your section so now let's add that new column by default adding a new column from the inspector adds it to the right of a grid but we can add one to the left by clicking this plus icon right on the canvas so we'll set it to pixels so it stays the same width across all screen sizes change the section background and add an element here and here so now the left column is set to 100 pixels and we've got two one FR columns if these grid measurements are in your way you can quickly toggle them off by pressing shift command or control and G then if we resize with the handles you can see that the fixed column will always take up 100 pixels of space from the section but the 1fr column split the remaining space in half and fill it no matter how much we scale down let's switch our measurements back on by clicking shift command or control in G again if you ever forget this shortcut remember you can find the setting by going to view canvas indications and grid measurements and set these other two columns to percent just to see what happens let's say 50 50. we'll see that this won't work because unlike fr the percentage unit takes its measurement from the entire section without taking the pixels into account so it's trying to take up 100 of the section's width causing an overflow of that 100 pixels to make this work without FR we'd need to figure out the exact percentage of the section remaining after the pixels which would be a lot of math overall using percentage here takes more effort and gives us less precision and control so let's switch back to fr now if we move back down to mobile you'll notice that the row we added on desktop hasn't appeared that's because we already edited the Grid on mobile earlier which created an override so let's add a new row on the bottom here by clicking the plus icon and then we can just place our new element into the new row and we'll give it a fixed pixel height too we'll want the second row to take up forty percent of the section height so let's change that and we'll finish up by setting that last row to FR so it takes up the remaining space now let's see how all that looks together [Music] you might prefer to start designing first and add a grid later like with this section we made earlier you can add grid lines after the fact by clicking on a section and then clicking the customize grid on canvas icon in this mode you can drag to adjust existing grid lines or you can create new grid Lines by dragging them in from the edges of a section when you drag these lines close to a piece of content they'll snap to it so it's easy to align them with your design and you can see the changes made in this mode won't affect the content in the section to delete any of these grid lines you can just right click and delete or drag them to the edge you originally took them from you can also delete them from the inspector now when you leave this mode you can use the grid as if it were any other CSS grid we've covered a lot in this course but now you're ready to put it all together to build and customize an advanced fully responsive section on your own that's what you'll do in your last challenge for our Essentials course I'll meet you there
